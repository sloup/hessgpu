INCLUDE_DIRECTORIES(${GLEW_INCLUDE_DIRS} ${SIFTGPU_INCLUDE_DIR}})

ADD_EXECUTABLE(hess hessgpucmd.cpp)
TARGET_LINK_LIBRARIES(hess ${TARGET_LIB_NAME})

INSTALL(TARGETS hess
        RUNTIME DESTINATION ${PROJECT_SOURCE_DIR}/bin
)

SET(CMAKE_VERBOSE_MAKEFILE ON)
